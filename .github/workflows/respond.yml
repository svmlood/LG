name: worker

on:
  push:

jobs:
  turbine:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        dump: ["dash", "Canada", "elastic", "certain", "explicitly"]
        nest: ["broadcast", "margin", "advance", "comedy"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar IR
